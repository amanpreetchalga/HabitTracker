package com.example.habittracker.ui.navigation

/**
 * Centralized route definitions to avoid string repetition and typos.
 */
object Routes {
    const val HABIT_LIST = "habit_list"
    const val HABIT_FORM = "habit_form"
}